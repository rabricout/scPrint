

grn_inferer = GNInfer(
    layer=[0, 1],
    batch_size=2,
    how="random expr",
    preprocess="softmax",
    head_agg="mean",
    filtration="none",
    forward_mode="none",
    num_genes=100,
    max_cells=10,
    doplot=False,
)
grn_adata = grn_inferer(model, adata)

